# key-remap-press-capslock
 Press Caps Lock when *powering111/key-remap* is enabled

# how to use
Use this command to compile.

    g++ main.cpp -o keymap-capslock.exe -mwindows

execute the binary once to send Caps Lock signal.